# Quickstart: Git Identity Validation

1. Run the generated-configuration assertions.
2. Confirm `lib.internal` has neither `name` nor `email`.
3. Confirm both Home Manager Git settings have neither `user.name` nor `user.email`.
4. Confirm Git, GitHub CLI, lazygit, and difftastic still evaluate as enabled.
