# Data Model: Desktop Boundaries

No application data entities are introduced. The relevant declarative values are an optional
host-owned Niri output fragment and the existing boolean GUI capability flag.
