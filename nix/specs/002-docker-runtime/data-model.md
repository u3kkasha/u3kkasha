# Data Model: Docker Runtime

No application data entities are introduced. Persistent Docker and Podman runtime stores are
external operational state; the feature deliberately performs no migration between them.
