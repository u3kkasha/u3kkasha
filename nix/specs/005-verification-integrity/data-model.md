# Data Model: Verification Integrity

No application data entities are introduced. Verification outputs are classified as quick unit
assertions, generated-configuration checks, host builds, and VM integration tests.
